# Smart Banking System — .env (generated by Ansible)
# DO NOT EDIT MANUALLY — managed by ansible/playbooks/02-deploy-app.yml

POSTGRES_USER=admin
POSTGRES_PASSWORD={{ postgres_password }}

REDIS_PASSWORD={{ redis_password }}

RABBITMQ_USER=admin
RABBITMQ_PASSWORD={{ rabbitmq_password }}

JWT_SECRET={{ jwt_secret }}
JWT_ACCESS_EXPIRY=86400000
JWT_REFRESH_EXPIRY=604800000

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME={{ smtp_username }}
SMTP_PASSWORD={{ smtp_password }}

APP_BASE_URL=http://{{ ansible_host }}
VITE_API_URL=/api
ALLOWED_ORIGIN=http://{{ ansible_host }}
