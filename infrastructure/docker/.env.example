# =============================================================
# Smart Banking System — Environment Variables Template
#
# INSTRUCTIONS:
#   1. Copy this file: cp .env.example .env
#   2. Fill in every value marked with CHANGE_ME
#   3. NEVER commit the real .env file to version control
#      (.env is already in .gitignore)
# =============================================================

# -------------------------------------------------------------
# PostgreSQL
# -------------------------------------------------------------
POSTGRES_USER=admin
POSTGRES_PASSWORD=micheljunior@1971

# -------------------------------------------------------------
# Redis
# -------------------------------------------------------------
REDIS_PASSWORD=micheljunior@1971
# -------------------------------------------------------------
# RabbitMQ
# -------------------------------------------------------------
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=micheljunior@1971

# -------------------------------------------------------------
# JWT — generate with: openssl rand -hex 32
# MUST be identical for auth-service and api-gateway
# -------------------------------------------------------------
JWT_SECRET=007324fae0def610655a1179cfe5207b8dcfd899b8f5f5737d3296b89cda2f61
JWT_ACCESS_EXPIRY=86400000
JWT_REFRESH_EXPIRY=604800000

# -------------------------------------------------------------
# SMTP — Gmail example (use an App Password, not your main pw)
# https://myaccount.google.com/apppasswords
# -------------------------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=tankemichel@gmail.com
SMTP_PASSWORD=iqhiqqbjthybiimr

# -------------------------------------------------------------
# Application URLs
# In production: replace localhost with your VPS domain/IP
# -------------------------------------------------------------
APP_BASE_URL=http://localhost
VITE_API_URL=/api
ALLOWED_ORIGIN=http://localhost
