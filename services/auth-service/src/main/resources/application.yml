server:
  port: 8081

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:5432/smart_wallet
    username: admin
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: your_super_secret_jwt_key_here