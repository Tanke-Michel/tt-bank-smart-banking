# =============================================================
# Auth Service — Docker profile override
# Replaces localhost references with container hostnames
# Active when: SPRING_PROFILES_ACTIVE=docker
# =============================================================
spring.datasource.url=jdbc:postgresql://postgres:5432/tt_bank_auth
spring.data.redis.host=redis
spring.data.redis.port=6379
app.base-url=${APP_BASE_URL:http://localhost}
