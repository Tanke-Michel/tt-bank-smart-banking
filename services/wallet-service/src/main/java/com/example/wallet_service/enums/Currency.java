package com.example.wallet_service.enums;

/**
 * Supported currencies.
 * XAF (Central African CFA franc) is the primary currency for Cameroon.
 */
public enum Currency {
    XAF,
    USD,
    EUR,
    GBP
}
